skips:
  - B101  # assert_used - asserts are okay in tests
  - B601  # paramiko_calls - not using paramiko
  - B603  # subprocess_without_shell_equals_true - subprocess usage is controlled

exclude_dirs:
  - '/tests/'
  - '/migrations/'
  - '/venv/'
  - '/.venv/'

confidence: medium
severity: medium